server:
  port: 8080

spring:
  application:
    name: trading-mvp-backend

  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&preparedStatementCacheQueries=0
    username: postgres.lcjfoxpbqyfnellhplyz
    password: jcrvis@123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: mySecretKeyForMVP12345678901234567890123456789012345678901234567890
  expiration: 86400000  # 24 hours in milliseconds

cors:
  allowed-origins: http://localhost:5173,http://localhost:5174

logging:
  level:
    com.trading.mvp: DEBUG
    org.springframework.security: DEBUG
